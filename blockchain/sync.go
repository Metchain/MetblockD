package blockchain

/*func (bc *Blockchain) SyncNodes() {
	bc.muxNeighbors.Lock()
	defer bc.muxNeighbors.Unlock()
	bc.SetNeighbors()
}

func (bc *Blockchain) StartSyncNodes() {
	bc.SyncNodes()
	_ = time.AfterFunc(time.Second*BLOCKCHAIN_NEIGHBOR_SYNC_TIME_SEC, bc.StartSyncNodes)
}

func (bc *Blockchain) Run() {
	bc.StartSyncNodes()
}
*/
